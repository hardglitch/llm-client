name: Release Builds (Multi-OS & Multi-Arch)

on:
  push:
    tags:
      - "v*"

env:
  CARGO_TERM_COLOR: always
  RUST_VERSION: stable

jobs:
  build-multi-os:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        include:
          # --- x86_64 ---
          - os: ubuntu-latest
            target: x86_64-unknown-linux-musl
            artifact-name: llm-client-linux-x86_64
            ext: ""
          - os: macos-latest
            target: x86_64-apple-darwin
            artifact-name: llm-client-macos-x86_64
            ext: ""
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            artifact-name: llm-client-windows-x86_64.exe
            ext: ".exe"

          # --- ARM64 ---
          - os: ubuntu-latest
            target: aarch64-unknown-linux-gnu
            artifact-name: llm-client-linux-arm64
            ext: ""
          - os: macos-latest
            target: aarch64-apple-darwin
            artifact-name: llm-client-macos-arm64
            ext: ""
          - os: windows-latest
            target: aarch64-pc-windows-msvc
            artifact-name: llm-client-windows-arm64.exe
            ext: ".exe"

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: ${{ env.RUST_VERSION }}
          targets: ${{ matrix.target }}

      - name: Install musl tools (Linux x86_64 only)
        if: matrix.os == 'ubuntu-latest' && matrix.target == 'x86_64-unknown-linux-musl'
        run: sudo apt-get update && sudo apt-get install -y musl-tools

      - name: Build release binary
        run: cargo build --release --target ${{ matrix.target }}

      - name: Prepare artifact (rename & move)
        shell: bash
        run: |
          BINARY_PATH="target/${{ matrix.target }}/release/llm-client${{ matrix.ext }}"
          ARTIFACT_NAME="${{ matrix.artifact-name }}"
          mv "$BINARY_PATH" "$ARTIFACT_NAME"
          echo "Created artifact: $ARTIFACT_NAME"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact-name }}
          path: ${{ matrix.artifact-name }}
          retention-days: 7

  create-release:
    needs: build-multi-os
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: releases

      - name: Create Release and upload assets
        uses: softprops/action-gh-release@v1
        with:
          name: "Release ${{ github.ref_name }}"
          body: |
            ## Changelog
            
            No changelog.
          files: |
            releases/llm-client-linux-x86_64
            releases/llm-client-linux-arm64
            releases/llm-client-macos-x86_64
            releases/llm-client-macos-arm64
            releases/llm-client-windows-x86_64.exe
            releases/llm-client-windows-arm64.exe
          token: ${{ secrets.GITHUB_TOKEN }}
